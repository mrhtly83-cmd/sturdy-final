### Step 1: Update`package.json`

1. Open your `package.json` file in the root of your Next.js project.
2. Look for a`"scripts"` section.If it doesn't exist, create one.
3. Here’s an example of what your `"scripts"` section might look like after adding or modifying scripts:
